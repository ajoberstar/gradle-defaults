
buildscript {
	repositories {
		jcenter()
		maven { url 'http://dl.bintray.com/ajoberstar/gradle-plugins' }
		maven { url 'http://dl.bintray.com/ajoberstar/temp-forks' }
	}
	dependencies {
		classpath 'org.ajoberstar:gradle-ajoberstar:0.2.0'
	}
}

apply plugin: 'groovy'
apply plugin: 'maven-publish'
apply plugin: 'bintray'
apply plugin: 'ajoberstar'

group = 'org.ajoberstar'

ajoberstar {
	friendlyName = 'Ajoberstar Gradle Plugin'
	description = 'Plugin for org.ajoberstar projects.'
	bintrayRepo = 'gradle-plugins'
	bintrayLabels = ['gradle']
	copyrightYears = '2014'
}

sourceCompatibility = '1.7'

repositories {
	jcenter()
	maven { url 'http://dl.bintray.com/ajoberstar/temp-forks' }
}

dependencies {
	compile gradleApi()
	compile localGroovy()
	compile 'org.ajoberstar:gradle-git:0.11.0-rc.2+3c2e190'
	compile 'org.ajoberstar:gradle-imports:0.1.0'
	compile 'nl.javadude.gradle.plugins:license-gradle-plugin:0.11.0'
	compile 'com.jfrog.bintray.gradle:gradle-bintray-plugin:0.4-fork+2014.03.28.05.40.09'
}

wrapper {
	gradleVersion = '2.1'
}
